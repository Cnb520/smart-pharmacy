services:
  - type: web
    name: smart-pharmacy
    env: node
    buildCommand: npm install && npm run build && cd server && npm install && npm run seed
    startCommand: cd server && npx tsx src/index.ts
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
